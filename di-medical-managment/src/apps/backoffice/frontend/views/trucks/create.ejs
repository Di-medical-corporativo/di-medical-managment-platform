<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Usuario</title>
  <link rel="stylesheet" href="/css/main.css">
  <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
</head>
<body>
  <div id="admin-dash">

    <%- include('../partials/_leftbar') %>
    
    <main id="main-admin">

      <header class="section-header">
        <section class="breadcrumb">
          <a href="/backoffice/truck">
            <small>Camionetas</small>
          </a>
          <span class="breadcrumb-slash">/</span>
          <a href="/backoffice/truck/new">
            <small>Nueva camioneta</small>
          </a>
        </section>
      </header>

      <section id="new-client-form">
        <form action="/backoffice/truck/<%= id %>" method="post" id="new-truck-form">
          <input type="text" hidden value="<%= id %>" name="id">
          
          <label for="plate">Placas:</label>
          <input type="text" id="plate" name="plate" class="input" required>
          
          <label for="model">Modelo:</label>
          <input type="text" name="model" id="model" class="input" required>
          
          <label for="brand">Marca:</label>
          <input type="text" name="brand" id="brand" class="input" required>
          
          <button type="submit" class="button primary">Nueva camioneta</button>
        </form>
      </section>
    </main>
  </div>

  <script>
    document.getElementById('new-truck-form').addEventListener('submit', function(event) {
      event.preventDefault();

      swal({
        title: '¿Estás seguro?',
        text: 'Estás a punto de crear una nueva camioneta. ¿Deseas continuar?',
        buttons: true,
        dangerMode: true,
      }).then((accepted) => {
        if (accepted) {
          this.submit();
        }
      });
    });
  </script>
</body>
</html>
