<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sin permisos suficientes</title>
  <style>
    /* Estilos básicos */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column; /* Permite que el logo y el mensaje se apilen */
      justify-content: center;
      align-items: center;
      background-color: #f8f9fa;
      color: #212529;
    }
    img {
      max-width: 150px; /* Tamaño máximo del logo */
      margin-bottom: 20px; /* Espacio entre el logo y el mensaje */
    }
    #auth-view {
      text-align: center;
      padding: 20px;
      border: 1px solid #dee2e6;
      border-radius: 5px;
      background-color: #fff;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    h2 {
      margin-bottom: 20px;
      color: #dc3545; /* Rojo para resaltar el mensaje */
    }
    a {
      text-decoration: none;
      color: #f8a900; /* Azul para destacar el enlace */
      font-weight: bold;
    }
    a:hover {
      text-decoration: underline; /* Subrayado al pasar el cursor */
    }
  </style>
</head>
<body>
  <img src="/images/logo.png" alt="Logo de la aplicación">
  <div id="auth-view">
    <h2>No tienes permiso para ver este módulo</h2>
    <a href="/backoffice">Regresa a tu perfil</a>
  </div>
</body>
</html>
